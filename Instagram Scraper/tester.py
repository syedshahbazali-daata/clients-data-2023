from instagramy import InstagramHashTag

# Provide a valid session ID
session_id = "your_session_id_here"

# Create an instance of the InstagramHashTag class
hashtag = InstagramHashTag("python", sessionid=session_id)

# Print out information about the hashtag
print(hashtag)
